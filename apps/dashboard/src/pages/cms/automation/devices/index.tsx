import { PagePlaceholder } from '../../../page-placeholder';

export default function AutomationDevicesPage() {
  return (
    <PagePlaceholder
      description="Device automation foundation placeholder."
      sections={['Discovered', 'Connected', 'Offline', 'Readiness']}
      title="Automation devices"
    />
  );
}
