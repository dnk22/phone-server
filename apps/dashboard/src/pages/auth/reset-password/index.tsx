import { PagePlaceholder } from '../../page-placeholder';

export default function ResetPasswordPage() {
  return (
    <PagePlaceholder description="Password reset foundation placeholder." title="Reset password" />
  );
}
