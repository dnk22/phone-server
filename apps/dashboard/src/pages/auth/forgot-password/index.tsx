import { PagePlaceholder } from '../../page-placeholder';

export default function ForgotPasswordPage() {
  return (
    <PagePlaceholder
      description="Password recovery request foundation placeholder."
      title="Forgot password"
    />
  );
}
